#!/bin/bash
# Model Configuration for Linux Platform (NVIDIA GPU)
# This file defines the AI models to be used on Ubuntu/Linux systems

# Primary general-purpose model
PRIMARY_MODEL="gpt-oss:20b"

# Code-specialized model
CODE_MODEL="codellama:34b"

# Embedding model for vector operations
EMBEDDING_MODEL="jeffh/intfloat-e5-base-v2:f16"

# Reranking model for search result optimization
RERANKING_MODEL="jeffh/intfloat-e5-base-v2:f16"

# Small general-purpose model for quick tasks
SMALL_MODEL="mistral:7b-instruct-q8_0"

# Additional models array for bulk operations
MODELS_TO_PULL=(
    "$PRIMARY_MODEL"
    "$CODE_MODEL"
    "$EMBEDDING_MODEL"
    "$RERANKING_MODEL"
    "$SMALL_MODEL"
)
