#!/bin/bash
# Model Configuration for Mac Platform (Apple Silicon)
# This file defines the AI models to be used on Mac systems

# Primary general-purpose model
PRIMARY_MODEL="gpt-oss:120b"

# Code-specialized model
CODE_MODEL="mychen76/qwen3_cline_roocode:14b"

# Embedding model for vector operations
EMBEDDING_MODEL="dengcao/Qwen3-Embedding-8B:Q5_K_M"

# Reranking model for search result optimization
RERANKING_MODEL="dengcao/Qwen3-Reranker-8B:Q5_K_M"

# Small general-purpose model for quick tasks
SMALL_MODEL="mistral:7b-instruct-q8_0"

# Additional models array for bulk operations
MODELS_TO_PULL=(
    "$PRIMARY_MODEL"
    "$CODE_MODEL"
    "$EMBEDDING_MODEL"
    "$RERANKING_MODEL"
    "$SMALL_MODEL"
)
